apiVersion: v1
kind: Service
metadata:
  name: fraud-detection
  namespace: fraud-detection
  labels:
    app: fraud-detection
spec:
  type: NodePort
  selector:
    app: fraud-detection
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30800